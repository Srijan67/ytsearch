const initialState = "";
const LoadAllVidoes = (state = initialState, action) => {
  switch (action.type) {
    case "LOAD_VIDEO":
      return state;
    default:
      return state;
  }
};
export default LoadAllVidoes;
